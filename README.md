# Визуальный конструктор блок-схем

Веб-приложение для создания и редактирования блок-схем алгоритмов с возможностью сохранения в базе данных и экспорта в PNG.

## 🚀 Технологии

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **SQLAlchemy** - ORM для работы с БД
- **SQLite** - легковесная база данных
- **JWT** - токены для авторизации
- **Bcrypt** - хеширование паролей

### Frontend
- **React** - UI библиотека
- **Tailwind CSS** - стилизация
- **Lucide React** - иконки

## 📁 Структура проекта

```
flowchart-constructor/
├── backend/
│   ├── main.py              # Основной файл FastAPI
│   ├── requirements.txt     # Зависимости Python
│   └── flowchart.db        # База данных SQLite (создается автоматически)
│
├── frontend/
│   ├── src/
│   │   └── App.jsx         # React приложение
│   ├── package.json
│   └── ...
│
└── README.md
```

## 🛠️ Установка и запуск

### Backend (FastAPI)

1. Создайте виртуальное окружение:
```bash
cd backend
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Запустите сервер:
```bash
python main.py
```

Сервер запустится на `http://localhost:8000`

API документация доступна на:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

### Frontend (React)

1. Установите Node.js и npm

2. Создайте React проект:
```bash
npx create-react-app frontend
cd frontend
```

3. Установите зависимости:
```bash
npm install lucide-react
```

4. Скопируйте содержимое `App.jsx` в `src/App.jsx`

5. Обновите `src/index.css`:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

6. Настройте Tailwind CSS:
```bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

7. В `tailwind.config.js`:
```js
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: { extend: {} },
  plugins: [],
}
```

8. Запустите приложение:
```bash
npm start
```

Приложение откроется на `http://localhost:3000`

## 📖 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Получить текущего пользователя

### Блок-схемы
- `POST /api/flowcharts` - Создать новую схему
- `GET /api/flowcharts` - Получить все схемы пользователя
- `GET /api/flowcharts/{id}` - Получить конкретную схему
- `PUT /api/flowcharts/{id}` - Обновить схему
- `DELETE /api/flowcharts/{id}` - Удалить схему

## 🎨 Функциональность

### Возможности редактора:
- ✅ Drag & drop перемещение блоков
- ✅ 4 типа блоков: Начало/Конец, Процесс, Условие, Ввод/Вывод
- ✅ Соединение блоков стрелками
- ✅ Редактирование текста блоков (двойной клик)
- ✅ Удаление блоков и связей
- ✅ Сохранение в базу данных
- ✅ Экспорт в PNG изображение

### Система пользователей:
- ✅ Регистрация и авторизация
- ✅ JWT токены
- ✅ Защищенные endpoints
- ✅ Личная библиотека схем

## 🔒 Безопасность

- Пароли хешируются с использованием bcrypt
- JWT токены для аутентификации
- CORS настроен для безопасности
- SQL инъекции предотвращены через ORM

## 📝 Использование

1. Зарегистрируйтесь или войдите в систему
2. Создайте новую блок-схему
3. Добавляйте блоки с боковой панели
4. Перетаскивайте блоки мышью
5. Соединяйте блоки: кликните на кнопку "→", затем на целевой блок
6. Редактируйте текст двойным кликом
7. Сохраняйте схему
8. Экспортируйте в PNG при необходимости

## ⚙️ Конфигурация

В `main.py` измените:
- `SECRET_KEY` - секретный ключ для JWT (ОБЯЗАТЕЛЬНО для продакшена!)
- `DATABASE_URL` - путь к базе данных
- `ACCESS_TOKEN_EXPIRE_MINUTES` - время жизни токена

В `App.jsx` измените:
- `API_URL` - адрес вашего API сервера

## 🐛 Troubleshooting

**Ошибка CORS:**
- Убедитесь, что backend запущен на `localhost:8000`
- Проверьте настройки CORS в `main.py`

**База данных не создается:**
- Убедитесь, что у вас есть права на запись в директорию
- Проверьте установку `aiosqlite`

**Frontend не подключается к backend:**
- Проверьте, что backend запущен
- Проверьте `API_URL` в `App.jsx`
- Откройте консоль браузера для ошибок

## 📄 Лицензия

MIT License - свободное использование для учебных и коммерческих целей

## 👨‍💻 Автор

Проект создан для учебных целей - визуализация алгоритмов и изучение современных веб-технологий